# DO NOT LEAVE TRAILING SPACES

# =============================================
# MySQL Datasource Configuration (JDBC ONLY)
# =============================================
quarkus.datasource.db-kind=mysql
quarkus.datasource.jdbc.url=jdbc:mysql://localhost:3306/codebiz_projects?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
quarkus.datasource.username=root
quarkus.datasource.password=23277868
quarkus.datasource.jdbc.driver=com.mysql.cj.jdbc.Driver

# JDBC pool sizing
quarkus.datasource.jdbc.min-size=2
quarkus.datasource.jdbc.max-size=10

# =============================================
# CORS CONFIG (Quarkus 3.x)
# =============================================
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=http://localhost:3000,http://localhost:5173
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.credentials=true
quarkus.http.cors.exposed-headers=location
quarkus.http.cors.access-control-max-age=24H

# =============================================
# JWT CONFIG
# =============================================
mp.jwt.verify.issuer=http://localhost:8080
mp.jwt.verify.publickey.location=publicKey.pem
quarkus.native.resources.includes=publicKey.pem
smallrye.jwt.sign.key.location=privateKey.pem